\hypertarget{index_8php_source}{\section{index.\-php}
}

\begin{DoxyCode}
00001 <?php
00002 define(\textcolor{stringliteral}{'CLASS\_DIR'}, \textcolor{stringliteral}{'classes/'});
00003 \textcolor{comment}{}
00004 \textcolor{comment}{/**}
00005 \textcolor{comment}{ * }
00006 \textcolor{comment}{ * PHP Klassen Autoloader definieren}
00007 \textcolor{comment}{ * }
00008 \textcolor{comment}{ * Andere Variante waere mit spl\_autoload\_register() }
00009 \textcolor{comment}{ * eine oder mehrere Funktionen definieren}
00010 \textcolor{comment}{ * }
00011 \textcolor{comment}{ * @param       String  $class  Klassenname}
00012 \textcolor{comment}{ * }
00013 \textcolor{comment}{ */}
00014 \textcolor{keyword}{function} \_\_autoload($class) \{
00015  
00016         \textcolor{comment}{// Namespace Backslashe in Slashe für das Filesystem umwandeln}
00017         $file = str\_replace(\textcolor{stringliteral}{"\(\backslash\)\(\backslash\)"}, DIRECTORY\_SEPARATOR, $class).\textcolor{stringliteral}{".php"};
00018  
00019         \textcolor{comment}{// Klasse einbinden ansonsten Exception werfen.}
00020         \textcolor{keywordflow}{if} (file\_exists(CLASS\_DIR . $file)) \{
00021                 require\_once CLASS\_DIR . $file;
00022         \} \textcolor{keywordflow}{else} \{
00023                 \textcolor{keywordflow}{throw} \textcolor{keyword}{new} Exception(\textcolor{stringliteral}{"Autoloaderfehler: "} . $file . \textcolor{stringliteral}{" not found."});
00024         \}
00025 \} 
00026  
00027 \textcolor{keywordflow}{try} \{
00028         
00029         \textcolor{comment}{// Session Initalisieren}
00030         $session = \(\backslash\)Session::getInstance();
00031         
00032         \textcolor{comment}{// Frontcontroller Initalsieren und Aufrufen}
00033         $c = new \(\backslash\)FrontController();
00034         $c->run();
00035         
00036                 
00037 \} \textcolor{keywordflow}{catch} (CustomException\(\backslash\)UserNotAuthedException $e) \{
00038         
00039         displayException($e, \textcolor{stringliteral}{"Illegaler Aufruf"});
00040         
00041 
00042 \} \textcolor{keywordflow}{catch} (CustomException\(\backslash\)InvalidUserPassException $e) \{
00043 
00044         displayException($e, \textcolor{stringliteral}{"Benutzername und/oder Passwort falsch"});
00045         
00046         
00047 \} \textcolor{keywordflow}{catch} (Exception $e)  \{       
00048         
00049         displayException($e, \textcolor{stringliteral}{"Das h&auml;tte nicht passieren d&uuml;rfen"});
00050         
00051 \}
00052 \textcolor{comment}{}
00053 \textcolor{comment}{/**}
00054 \textcolor{comment}{ * }
00055 \textcolor{comment}{ * Exception Anzeigen}
00056 \textcolor{comment}{ *  }
00057 \textcolor{comment}{ * @param Exception $e  Exception Klasse}
00058 \textcolor{comment}{ * @param String        $h1     Ueberschrift}
00059 \textcolor{comment}{ * }
00060 \textcolor{comment}{ */}
00061 \textcolor{keyword}{function} displayException($e, $h1) \{
00062         
00063         $view = new \(\backslash\)View();
00064         $view->setTemplate(\textcolor{stringliteral}{'exception'});
00065         $view->assign(\textcolor{stringliteral}{'h1'}, $h1);
00066         $view->assign(\textcolor{stringliteral}{'exception'}, $e->getMessage());
00067         $view->display();
00068         
00069 \}
00070 
00071 $conf = \(\backslash\)Config::getInstance();
00072 
00073 \textcolor{comment}{// Debugging}
00074 \textcolor{keywordflow}{if} ($conf->application\_debugging == 1) \{
00075         print \textcolor{stringliteral}{"<br /><br />"};
00076         print \textcolor{stringliteral}{"<pre>"};
00077         print \textcolor{stringliteral}{'$\_GET '};
00078         print\_r($\_GET);
00079         print \textcolor{stringliteral}{'$\_POST '};
00080         print\_r($\_POST);
00081         print \textcolor{stringliteral}{'$\_SESSION '};
00082         print\_r($\_SESSION);
00083         print \textcolor{stringliteral}{'$\_SERVER '};
00084         print\_r($\_SERVER);      
00085         print \textcolor{stringliteral}{"</pre>"};
00086 \}
00087 
00088 
00089 ?>
\end{DoxyCode}
